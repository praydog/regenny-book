# sdkgenny.GenericType

Inherits from [Type](type.md).

A generic/opaque type with no additional methods beyond those inherited from [Type](type.md). Used to represent types that don't fit into the struct/enum/reference categories.
